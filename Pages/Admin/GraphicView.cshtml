@page
@model KosmosCore.Pages.Admin.GraphicViewModel
@{
    ViewData["Title"] = "Графики";
    Layout = "_AdminLayout";
}

<div class="admin-card">
    <div class="admin-card-toolbar">
        <h2 class="admin-card-title admin-card-title--flush">Просмотр графиков (Аналитика)</h2>
        <button class="site-nav__btn site-nav__btn--ghost">Экспорт PDF</button>
    </div>
    
    <div id="graphics-container" class="admin-graphics-container">
        <canvas id="myChart" width="400" height="400"></canvas>
    </div>
</div>
