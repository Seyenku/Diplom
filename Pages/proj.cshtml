@page "/game"
@model KosmosCore.Pages.ProjModel
@{
    ViewData["Title"] = "Stellar Vocation";
    Layout = "_GameLayout";
    // Сериализованные данные Bootstrap передаются через ViewData в _GameLayout
    ViewData["GameInitData"] = Model.InitialDataJson;
}
